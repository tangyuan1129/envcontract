# Contributing

1. Fork the repository.
2. Make one focused change.
3. Run `npm test` and `node bin/envcontract.js . --strict`.
4. Open a pull request using the template.

Please include a small fixture or test for new language patterns. Keep the CLI dependency-free and never add code that uploads repository contents.

